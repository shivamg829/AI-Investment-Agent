# UI Fix - TODO

- [x] Update `frontend/src/components/ReportCard.jsx` to be resilient to missing/partial AI response fields (guard `positives`, `risks`, `summary`, `decision`, `sources`).
- [ ] (If still needed) Add `.decision.review` styling in `frontend/src/index.css` so the fallback pill looks intentional.
- [x] Run frontend build/dev to ensure no runtime errors (after UI change).
- [x] Fix frontend lint error in `frontend/src/pages/Home.jsx`.





